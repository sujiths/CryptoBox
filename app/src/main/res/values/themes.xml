<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CryptoBox.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar"></style>
    <style name="Theme.CryptoBox.Dialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowBackground">@drawable/dialog_theme</item>
        <item name="android:windowMinWidthMinor">85%</item>
    </style>

    <style name="Theme.CryptoBox.ToolbarTheme" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="actionMenuTextColor">@android:color/white</item>
    </style>
</resources>